<div class="mini-cards-admin" id="mini-cards-admin">
  <h4>Secțiunea pentru mini carduri</h4>
    <div class="new-minicard-section">
      <h5>Minicard nou</h5>
      <div class="new-minicard-section__headers">
        <p>Selectează</p>
        <p class="text-center">Text</p>
        <p>Acțiune</p>
      </div>
      <%= simple_form_for(mc) do |f| %>
      <div class="new-minicard-section__form">
          <span><%= f.input :photo, as: :file, label: false %></span>
          <span><%= f.input :text, label: false %></span>
          <span><%= f.submit 'Card nou' , class: "btn btn-sm btn-primary #{"disabled" if @mini_cards.count > 2 && !mc.id }" %></span>
        </div>
        <% end %>
    </div>
    <div class="minicards-index-section">
      <h5>Carduri pentru carousel existente</h5>
      <div class="minicards-index-section__headers">
        <p>Imagine</p>
        <p>Selectează</p>
        <p class="text-center">Text</p>
        <p>Acțiune</p>
        <p>Șterge</p>
      </div>
      <% @mini_cards.each_with_index do |mc, i| %>
        <%= simple_form_for(mc) do |f| %>
          <div class="minicards-index-section__form">
            <span><%= cl_image_tag(mc.photo.key, height: 100, width: 100, crop: :fill) if mc.photo.key.present? %></span>
            <span><%= f.input :photo, as: :file, label: false %></span>
            <span><%= f.input :text, label: false, class: 'text-center' %></span>
            <span><%= f.submit 'Modifică', class: "btn btn-sm btn-primary" %></span>
            <span><%= link_to '🗑', mini_card_path(mc), method: :delete %></span>
          </div>
        <% end %>
      <% end %>
    </div>
</div>